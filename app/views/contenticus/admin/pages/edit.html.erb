<h1> Edit Page </h1>

<%= bootstrap_form_for @page, url: contenticus_admin_page_path, method: 'put' do |f| %>

  <%= render 'layouts/contenticus/admin/formbar', f: f %>

  <%= render 'form', f: f %>

<% end %>