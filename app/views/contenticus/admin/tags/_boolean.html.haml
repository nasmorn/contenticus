- css_class = tag.hidden? ? 'form-group hidden' : 'form-group'
= form.fields_for tag.key, tag do |tag_form|
  %div{class: css_class}
    .col-sm-10.col-sm-push-2
      = tag_form.check_box :value, label: tag.name
